{
  "_comment_crop": "======================== crop ========================",
  "crop ()": {
    "prefix": "crop",
    "body": [
      "crop = function()",
      "\twhile not self[self.len - 1].len > 0",
      "\t\tself.remove(self.len - 1)",
      "\tend while",
      "\twhile not self[0].len > 0",
      "\t\tself.remove(0)",
      "\tend while",
      "\treturn self",
      "end function"
    ],
    "description": "Define .crop()"
  },
  "_comment_push_once": "======================== push_once ========================",
  "push_once (define)": {
    "prefix": "push_once",
    "body": [
      "push_once = function(listItem)",
      "\tif not self.hasIndex(self.indexOf(locals.listItem)) then",
      "\t\tself.push(locals.listItem)",
      "\tend if",
      "end function"
    ],
    "description": "Define push_once(listItem)"
  },
  "_comment_string": "======================== string ========================",
  "string (define)": {
    "prefix": "string",
    "body": [
      "list.string = function(seperator = char(10))",
      "\tlocals.printString = split(self, locals.seperator)",
      "\t\treturn locals.printString",
      "end function"
    ],
    "description": "Define list.string(seperator = char(10)"
  }
}